//arguments

function multiply (num1, num2){//eigula je bole parameters
            const result = num1*num2;
            console.log('Multiplication result:', result)
}

multiply(45, 2);// eitake bole argument

function square (number){//parameter
            const borgo = number*number;
            console.log('Sqare of the number:', borgo);
}

square(25);//arguments
const murgi =26;
square(murgi);//variable diye call kora eita .